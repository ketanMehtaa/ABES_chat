<li>
    <p>
        <small>

            <% if (locals.user && ( locals.user.id == comment.user.id || locals.user.id==comment.post.id)) { %>
            <a href="/comments/destroy/<%=comment.id%>">X</a>
            <% } %>
        </small>

        <%= comment.content %>
        <br>
        <small>
            <%= comment.user.name %>
        </small>

        <!-- display the likes of this comment if the user is logged in then show the link to toggle likes else just show the count 
        study the data attribute -->
        <small>
            <% if(locals.user) {%>
               <a href="/likes/toggle/?id=<%=comment._id%>&type=Comment" data-likes="<%=comment.likes.length %>" class="toggle-like-button" >
               <%= comment.likes.length %> Likes
               </a>
            <% } else { %>
               <%= comment.likes.length %> Likes         
            <% } %>
         </small>
    </p>
</li>